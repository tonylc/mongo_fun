<%= form_for([@claim, @customer]) do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% @customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class=field>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</div>
	<div class=field>
		<%= f.label :postcode %><br>
		<%= f.text_field :postcode %>
	</div>
	<div class=field>
		<%= f.label :phone %><br>
		<%= f.text_field :phone %>
	</div>
	<div class=field>
		<%= f.label :dob %><br>
		<%= f.text_field :dob %>
	</div>
  <div class=actions>
    <%= f.submit %>
  </div>
<% end %>
