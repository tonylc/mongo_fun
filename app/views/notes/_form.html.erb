<%= form_for([@claim, @note]) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class=field>
		<%= f.label :author_name %><br>
		<%= f.text_field :author_name %>
	</div>
	<div class=field>
		<%= f.label :body_text %><br>
		<%= f.text_area :body_text %>
	</div>
  <div class=actions>
    <%= f.submit %>
  </div>
<% end %>
